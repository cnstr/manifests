apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: typesense
  namespace: canister
spec:
  storageClassName: openebs-hostpath
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
